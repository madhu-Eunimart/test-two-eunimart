const CANCEL = "cancel";
const CONFIRM = "confirm";
const INIT = "init";
const SEARCH = "search";
const SELECT = "select";
const STATUS = "status";
const SUPPORT = "support";
const TRACK = "track";
const ISSUE = "issue";

const BPP_API_URLS = {
    CANCEL,
    CONFIRM,
    INIT,
    SEARCH,
    SELECT,
    STATUS,
    SUPPORT,
    TRACK,
    ISSUE,
}

export default BPP_API_URLS;